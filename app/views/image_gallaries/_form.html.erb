<%= form_for(@image_gallary) do |f| %>
  <% if @image_gallary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_gallary.errors.count, "error") %> prohibited this image_gallary from being saved:</h2>

      <ul>
      <% @image_gallary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_id %><br>
    <%= f.text_field :image_id %>
  </div>
  <div class="field">
    <%= f.label :references %><br>
    <%= f.text_field :references %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
